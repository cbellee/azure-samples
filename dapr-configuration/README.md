# Azure Container App - Dapr configuration spec component issue reproduction

## Requirements

- Azure Subscription
- Azure CLI

## Steps to reproduce

- clone this repo to your local machine
- login to the Azure CLI
  - `$ az login`
- Run the deployment script
  - `$ ./build.sh`
